name: self-hosted-check

on:
  workflow_dispatch:

jobs:
  smoke:
    runs-on: [self-hosted, linux, x64, jenkins-docker]

    steps:
      - name: Confirm runner
        run: |
          echo "Runner is online"
          hostname
          docker --version
